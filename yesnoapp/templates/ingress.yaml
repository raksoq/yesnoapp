{{- if .Values.yesnoapp.ingress.enabled -}}
{{- $fullName := include "yesnoapp.fullname" . -}}
{{- $svcPort := .Values.yesnoapp.service.port -}}  # Adjusted to refer to yesnoapp service port
{{- if and .Values.yesnoapp.ingress.className (not (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.yesnoapp.ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.yesnoapp.ingress.annotations "kubernetes.io/ingress.class" .Values.yesnoapp.ingress.className}}
  {{- end }}
{{- end }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}
  labels:
    {{- include "yesnoapp.labels" . | nindent 4 }}
  {{- with .Values.yesnoapp.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.yesnoapp.ingress.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.yesnoapp.ingress.className }}
  {{- end }}
  {{- if .Values.yesnoapp.ingress.tls }}
  tls:
    {{- range .Values.yesnoapp.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.yesnoapp.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
    {{- end }}
{{- end }}
