apiVersion: v1
kind: Service
metadata:
  name: {{ include "yesnoapp.fullname" . }}
  labels:
    {{- include "yesnoapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "yesnoapp.selectorLabels" . | nindent 4 }}
